<% content_for :head do %>
  <title><%= @user.name %> - Monolist</title>
  <meta name="description" content="<%= @user.name %>の持っているモノ、欲しいモノ、別に知りたくないとは思いますが教えちゃいます。"/>
<% end %>

<%= render 'profile' , user: @user %>


<%= render 'items/list' , items: @items %>